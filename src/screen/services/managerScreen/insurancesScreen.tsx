import { Text, View } from "react-native";

export const InsurancesScreen = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>insurance Screen</Text>
        </View>
    );
}