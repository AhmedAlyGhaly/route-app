import { Text, View } from "react-native";

export const InsuranceData = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Insurance data</Text>
        </View>
    );
}