import { Button, Text, View } from "react-native";

export const Financial = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            {/* <Button
                title="Personal"
                onPress={() => navigation.navigate('Personal')}
            /> */}
            <Text>financial Screen</Text>
        </View>
    );
}