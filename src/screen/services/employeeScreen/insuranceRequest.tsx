import { Text, View } from "react-native";

export const InsuranceRequest = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Insurance Request</Text>
        </View>
    );
}